# Copy this file to ".env" and paste your YouTube Data API v3 key.
# Get one at https://console.cloud.google.com/ -> APIs & Services -> Credentials.
# Restrict the key by HTTP referrer (your dev + deploy URLs) since Vite exposes it client-side.
VITE_YOUTUBE_API_KEY=
